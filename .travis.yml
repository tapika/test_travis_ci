language: cpp

matrix:
  include:
  - os: linux
    dist: bionic
  - os: windows

script:
- python build.py

before_stage: 
    - if [ "$TRAVIS_OS_NAME" = "windows" ]; then mkdir /c/dev1 && cd /c/dev1; fi

notifications:
  email: false
