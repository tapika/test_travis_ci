language: cpp

matrix:
  include:
  - os: linux
    dist: bionic
  - os: windows

git:
  clone: false

script: 
    - if [ "$TRAVIS_OS_NAME" = "windows" ]; then mkdir /c/dev1 && cd /c/dev1; fi
    - git clone https://github.com/tapika/test_travis_ci.git test_travis_ci && cd test_travis_ci
    - python build.py

notifications:
  email: false
